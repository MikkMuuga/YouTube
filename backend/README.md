# Backend (Express)

Minimal Express backend for the project (no database).

Quick start

```bash
cd backend
npm install
npm run dev
npm start
```

Available endpoints:
- GET /api/hello
- POST /api/echo (JSON body echoed back)

Tip: In development the Vite frontend is configured to proxy `/api` to the backend.
